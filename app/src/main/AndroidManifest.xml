<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.fx.manage">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".base.AppApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".ui.SplashActivity"
            android:theme="@style/AppTheme.Launcher" />
        <activity android:name=".ui.login.view.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.adminstrator.view.activity.QueryPublishHouseActivity"
            android:theme="@style/MyDialogStyle" />
        <activity android:name=".ui.student.view.activity.StudentActivity" />
        <activity android:name=".ui.adminstrator.view.activity.AdminActivity" />
        <activity android:name=".ui.student.view.activity.Stu_BookDetailActivity" />
        <activity android:name=".ui.adminstrator.view.activity.Admin_BookDetailActivity" />
        <activity android:name=".ui.student.view.activity.PersonalDataActivity" />
        <activity android:name=".ui.student.view.activity.CollectionActivity" />
        <activity android:name=".ui.adminstrator.view.activity.DeleteBookActivity" />
        <activity android:name=".ui.adminstrator.view.activity.QueryBookActivity" />
        <activity android:name=".ui.adminstrator.view.activity.QueryUserActivity" />
        <activity android:name=".ui.adminstrator.view.activity.ShouyeActivity" />
    </application>

</manifest>